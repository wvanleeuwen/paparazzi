

Update:

all flightplans:

	- replace estimator_z -> GpsPosAlt()

all airframes:

	- replace CONTROL_RATE -> CONTROL_FREQUENCY

fixedwing airframes:

	- add <subsystem name="ins" type="alt_float" /> if there is an ALT_KALMAN(_ENABLED) define or if you use a Baro
	- add <subsystem name="ins" type="gps_passthrough" /> to use direct GPS data (remove all ALT_KALMAN)
	- ALT_KALMAN -> ALT_KALMAN_ENABLED = TRUE


all conf/boards.makefile:

	- add $(TARGET).LDSCRIPT
	- replace ifndef bij XXX_LED en ports by ?=
	- add 
		#
		# default actuator configuration
		#
		# you can use different actuators by adding a configure option to your firmware section
		# e.g. <configure name="ACTUATORS" value="actuators_ppm/>
		#
		ifndef ACTUATORS
		ACTUATORS = actuators_pwm
		endif


all sw/airborne/boards.h

	- add EXT_CLK
	- change to openlocm3
	- Default actuator driver
	- ADC_CHANNEL_X -> X


