<!DOCTYPE flight_plan SYSTEM "../flight_plan.dtd">

<flight_plan
        name="Flight Plan"
        lat0="51.990634" lon0="4.376789"
        ground_alt="0"
        security_height="0.4"
        max_dist_from_home="8"
        alt="2.0">

  <header>
    #include "guidance/guidance_h.h"
    #include "guidance/guidance_v.h"
    #include "modules/flight_plan_guided/flight_plan_guided.h"
    #include "subsystems/datalink/datalink.h"
    #include "subsystems/radio_control.h"
  </header>

  <waypoints>
    <!-- Mission waypoints -->
    <waypoint name="HOME" x="0.0" y="0.0"/>
  </waypoints>

  <exceptions>
    <exception cond="datalink_time > 5" deroute="Land here"/>
    <exception cond="radio_control.status == RC_REALLY_LOST" deroute="Land here"/>
    <exception cond="stateGetPositionEnu_f()->z > 8." deroute="Land here"/>
  </exceptions>

  <blocks>
    <block name="Wait">
      <while cond="1"/>
    </block>

    <!-- Wait 5 seconds and then set altitude reference -->
    <block name="Geo init">
      <call_once fun="ResetAlt()"/>
      <while cond="1"/>
    </block>

    <block name="HOVER BEFORE BLUE ITEM">
      <call_once fun="front_cam_set_x_offset(512)"/>
      <call_once fun="detector_disable_all()"/>
      <call_once fun="detector.front_blue_item->active = true"/>
      <call_once fun="detector.bottom_blue_item->active = true"/>
      <call_once fun="open_gripper()"/>
      <call_once fun="object_state = 0"/>
      <call fun="Hover(NOM_FLIGHT_ALT)"/>
      <while cond="1"/>
    </block>

    <block name="Find and land at blue item">
      <call fun="go_to_object(true)"/>
    </block>

    <block name="Grab blue item">
      <call_once fun="close_gripper()"/>
      <while cond="2 > block_time"/>
    </block>

    <block name="Lift up blue item">
      <call_once fun="TakeOff(0.2)"/>
      <while cond="2 > block_time"/>
    </block>

    <block name="HOVER AFTER BLUE ITEM">
      <call fun="Hover(NOM_FLIGHT_ALT)"/>
      <while cond="1"/>
    </block>

    <block name="HOVER BEFORE RED ITEM">
      <call_once fun="front_cam_set_x_offset(512)"/>
      <call_once fun="detector_disable_all()"/>
      <call_once fun="detector.front_red_item->active = true"/>
      <call_once fun="detector.bottom_red_item->active = true"/>
      <call_once fun="open_gripper()"/>
      <call_once fun="object_state = 0"/>
      <call fun="Hover(NOM_FLIGHT_ALT)"/>
      <while cond="1"/>
    </block>

    <block name="Find and land at red item">
      <call fun="go_to_object(true)"/>
    </block>

    <block name="Grab red item">
      <call_once fun="close_gripper()"/>
      <while cond="2 > block_time"/>
    </block>

    <block name="Lift up red item">
      <call_once fun="TakeOff(0.2)"/>
      <while cond="2 > block_time"/>
    </block>

    <block name="HOVER AFTER RED ITEM">
      <call fun="Hover(NOM_FLIGHT_ALT)"/>
      <while cond="1"/>
    </block>

    <block name="HOVER BEFORE BLUE BUCKET">
      <call_once fun="detector_disable_all()"/>
      <call_once fun="front_cam_set_x_offset(512)"/>
      <call_once fun="detector.front_blue_bucket->active = true"/>
      <call_once fun="detector.bottom_blue_bucket->active = true"/>
      <call_once fun="close_gripper()"/>
      <call_once fun="object_state = 0"/>
      <call fun="Hover(NOM_FLIGHT_ALT)"/>
      <while cond="1"/>
    </block>

    <block name="Find and hover above blue bucket">
      <call fun="go_to_object(false)"/>
    </block>

    <block name="Release blue item">
      <call_once fun="open_gripper()"/>
    </block>

    <block name="HOVER AFTER BLUE BUCKET">
      <call fun="Hover(NOM_FLIGHT_ALT)"/>
      <while cond="1"/>
    </block>

    <block name="HOVER BEFORE RED BUCKET">
      <call_once fun="detector_disable_all()"/>
      <call_once fun="front_cam_set_x_offset(512)"/>
      <call_once fun="detector.front_red_bucket->active = true"/>
      <call_once fun="detector.bottom_red_bucket->active = true"/>
      <call_once fun="close_gripper()"/>
      <call_once fun="object_state = 0"/>
      <call fun="Hover(NOM_FLIGHT_ALT)"/>
      <while cond="1"/>
    </block>

    <block name="Find and hover above red bucket">
      <call fun="go_to_object(false)"/>
    </block>

    <block name="Release red item">
      <call_once fun="open_gripper()"/>
    </block>

    <block name="HOVER AFTER RED BUCKET">
      <call fun="Hover(NOM_FLIGHT_ALT)"/>
      <while cond="1"/>
    </block>

    <block name = "Land here">
      <call fun="guidance_h_set_guided_pos_relative(0.,0.)"/>
      <call fun="guidance_v_set_guided_th(0.4)"/>
      <!--deroute block="Hover"/-->
    </block>

    <block name="TESTING - Open gripper">
      <call_once fun="open_gripper()"/>
      <while cond="1"/>
    </block>

    <block name="TESTING - Close gripper">
      <call_once fun="close_gripper()"/>
      <while cond="1"/>
    </block>
    
    <block name="TESTING - Hover forever">
      <call fun="Hover(NOM_FLIGHT_ALT)"/>
      <while cond="1"/>
    </block>

    <block name="TESTING - Hover over red bucket">
      <call_once fun="detector_disable_all()"/>
      <call_once fun="detector.bottom_red_bucket->active = true"/>
      <call fun="Hover(NOM_FLIGHT_ALT)"/>
      <call fun="marker_center_descent(0, 0, 0)"/>
    </block>

  </blocks>




</flight_plan>
