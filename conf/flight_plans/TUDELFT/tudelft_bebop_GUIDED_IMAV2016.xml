<!DOCTYPE flight_plan SYSTEM "../flight_plan.dtd">

<flight_plan
        name="Flight Plan"
        lat0="51.990634" lon0="4.376789"
        ground_alt="0"
        security_height="0.4"
        max_dist_from_home="8"
        alt="2.0">

  <!--<header>-->
    <!--#include "autopilot.h"-->
    <!--#include "guidance/guidance_h.h"-->
    <!--#include "guidance/guidance_v.h"-->
    <!--#include "subsystems/ahrs.h"-->
    <!--#include "subsystems/electrical.h"-->
    <!--#include "subsystems/datalink/datalink.h"-->
  <!--</header>-->

  <waypoints>
    <!-- Mission waypoints -->
    <waypoint name="HOME" x="0.0" y="0.0"/>
  </waypoints>

  <!--<exceptions>-->
    <!--<exception cond="datalink_time > 2 && nav_is_in_flight()" deroute="Hover"/>-->
  <!--</exceptions>-->

  <blocks>
    <!-- Kill the throttle and wait for GPS fix -->
    <block name="Wait GPS">
      <call fun="KillEngines()"/>
      <while cond="!GpsFixValid()"/>
    </block>

    <!-- Wait 5 seconds and then set altitude reference -->
    <block name="Geo init">
      <call fun="ResetAlt()"/>
      <while cond="1"/>
    </block>

    <block name="TakeOff">
      <call fun="StartEngines()"/>
      <call fun="Hover(2.5)"/>
      <call fun="RotateToHeading(0)"/> <!-- not yet working -->
    </block>

     <!--Hover at the current location-->
    <block name="Hover">
      <call fun="Hover(2.5)"/>
      <while cond="1"/>
      <!--<exception cond="" deroute="Move Forward"/>-->
      <exception cond="marker1.detected && marker1.found_time > 3.0" deroute="Experimental Landing"/>
    </block>

    <block name="Experimental Landing">
      <call fun="Land(0.1)"/>             <!-- this call blocks until altitude < 0.1 -->
      <call_once fun="KillEngines()"/>
      <while cond="1"/>                   <!-- wait indefinitely -->
    </block>

    <block name="Detect Landing Pad">
      <call_once fun="detector_locate_helipad()"/>
      <while cond="1"/>
    </block>

    <block name="Detect Bucket">
      <call_once fun="detector_locate_bucket()"/>
      <while cond="1"/>
    </block>


    <!-- Start the bucket mission -->
    <!--<block name="Start Bucket">-->
      <!--<call fun="start_color_tracking_front()"/>-->
      <!--<call fun="start_color_tracking_bottom()"/>-->
      <!--<while cond="1"/>-->
    <!--</block>-->

    <!--&lt;!&ndash; Stop the bucket mission &ndash;&gt;-->
    <!--<block name="Stop Bucket">-->
      <!--<call fun="stop_color_tracking_front()"/>-->
      <!--<call fun="stop_color_tracking_bottom()"/>-->
      <!--<deroute block="Hover"/>-->
    <!--</block>-->

    <!--&lt;!&ndash; Start the landing on a moving platform &ndash;&gt;-->
    <!--<block name="Start LandingMovingPlatform">-->
      <!--<call fun="start_helipad()"/>-->
      <!--<call fun="MoveForward(0.1)"/>-->
      <!--<while cond="1"/>-->
      <!--<exception cond="marker" deroute="Land"/>-->
    <!--</block>-->

    <!--<block name="Land">-->
      <!--<call fun="start_autonomous_landing()"/>-->
      <!--<while cond="1"/>-->
    <!--</block>-->

  </blocks>
</flight_plan>
